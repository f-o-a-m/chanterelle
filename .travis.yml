language: node_js
dist: trusty
sudo: required
node_js: stable
install:
  - npm install
  - bower install --production
script:
  - pulp build
  - npm run docs;
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: docs/_build/html
  on:
    branch: master
